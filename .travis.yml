addons:
  sauce_connect: true

language: java
script: mvn -DseleniumGridUrl=http://username:access_key@ondemand.saucelabs.com:80/wd/hub -DseleniumCapabilities="browserName:chrome, tunnel-identifier:$TRAVIS_JOB_NUMBER" test

after_success:
  - "[[ $TRAVIS_BRANCH == \"master\" ]] && { python addServer.py; mvn -DskipTests deploy --settings ~/.m2/mySettings.xml; };"
